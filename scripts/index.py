"""Gera embeddings e indexa os chunks no pgvector (HNSW + FTS). Etapa 2 (a implementar)."""

if __name__ == "__main__":
    raise SystemExit("Etapa 2 ainda não implementada: embeddings + indexação.")
